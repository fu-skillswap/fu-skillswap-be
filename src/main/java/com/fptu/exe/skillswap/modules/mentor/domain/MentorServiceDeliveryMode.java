package com.fptu.exe.skillswap.modules.mentor.domain;

/** A group offer is deliberately a separate service from a direct 1:1 offer. */
public enum MentorServiceDeliveryMode {
    ONE_TO_ONE,
    GROUP_SESSION
}
