package com.fptu.exe.skillswap.modules.blog.domain;

/** Content ownership, deliberately independent from the author's authentication roles. */
public enum BlogAuthorType {
    MENTOR,
    PLATFORM
}
